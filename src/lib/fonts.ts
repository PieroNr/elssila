import localFont from "next/font/local";

export const cabinet = localFont({
  src: "../../public/fonts/Cabinet.ttf",
  variable: "--font-cabinet",
  display: "swap",
  preload: true,
});

export const neima = localFont({
  src: "../../public/fonts/Neima.ttf",
  variable: "--font-neima",
  display: "swap",
  preload: true,
});

